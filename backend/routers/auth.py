"""
Auth router — POST /auth/register, POST /auth/login, GET /auth/me
Implements ARCHITECTURE.md Authentication endpoints.
"""

from fastapi import APIRouter

router = APIRouter()


# TODO: [TICKET-005] Implement auth endpoints
# POST /register — UserCreate → AuthResponse
# POST /login — LoginRequest → AuthResponse
# GET /me — (Bearer) → UserResponse
