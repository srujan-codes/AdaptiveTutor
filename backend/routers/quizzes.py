"""
Quizzes router — POST /quizzes/generate, POST /quizzes/submit
Implements ARCHITECTURE.md Quizzes endpoints.
"""

from fastapi import APIRouter

router = APIRouter()


# TODO: [TICKET-013] Implement quiz endpoints
# POST /generate — QuizGenerateRequest → { questions: list[QuizQuestionResponse] }
# POST /submit — QuizSubmitRequest → QuizSubmitResponse
